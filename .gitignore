
# Amostras de voz do paciente — dado biométrico, nunca versionar
voz/
*.ogg
*.wav
qrcode.png

# Dados de saúde identificáveis do paciente — nunca versionar
conhecimento/
voz/
backup_fatos_*.json

# Bytecode Python — gerado localmente (testes, execução), nunca versionar
__pycache__/
*.pyc
